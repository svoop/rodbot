# frozen-string-literal: true

module Rodbot
  module Constants

    # Service types
    SERVICES = %i(app relay schedule).freeze

    # Hosting types
    HOSTINGS = %i(docker render).freeze

  end
end
